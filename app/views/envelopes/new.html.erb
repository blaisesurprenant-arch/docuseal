<h1 class="text-2xl font-bold mb-4">New Envelope for <%= @transaction.name %></h1>
<%= render 'form', envelope: @envelope, templates: @templates, selected_template_ids: [],
                    submit_label: 'Next: Assign Roles' %>
