<h1 class="text-2xl font-bold mb-4">Edit Envelope for <%= @transaction.name %></h1>
<%= render 'form', envelope: @envelope, templates: @templates, selected_template_ids: @selected_template_ids,
                    submit_label: 'Save' %>
